#include "main.h"
#include<stdio.h>
/**
 * _abs - computes the absolute value of an integer
 *
 * @n: the integer to compute the absolute value.
 *
 * Return: the absolute value of the input value of integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		return -n;
	}
	else
	{
	return n;
	}
}
